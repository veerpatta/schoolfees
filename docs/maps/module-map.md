# Module map

**Superseded by `src/modules/README.md`** — which lists every module, one line
each, and links to a README per module saying what it owns, its invariants, and
what must never happen there.

The structural half — how many files, which routes — is generated into
[`repo-map.md`](repo-map.md) and checked in CI.

This file held a third copy of the same list. See [`folder-map.md`](folder-map.md)
for why that stopped being a good idea.
