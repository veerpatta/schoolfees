import { RouteLoading } from "@/ui/shell/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Student statement"
      description="Building the fee statement."
      variant="table"
    />
  );
}
