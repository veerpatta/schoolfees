import { RouteLoading } from "@/ui/shell/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Family statement"
      description="Building the family fee statement."
      variant="table"
    />
  );
}
