import { RouteLoading } from "@/ui/shell/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Money glossary"
      description="Loading the definitions used across the app."
      variant="cards"
    />
  );
}
