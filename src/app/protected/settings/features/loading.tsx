import { RouteLoading } from "@/ui/shell/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Feature flags"
      description="Loading who can see each School One surface."
      variant="cards"
    />
  );
}
