import { RouteLoading } from "@/ui/shell/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Fee structure"
      description="Loading the fee structure."
      variant="cards"
    />
  );
}
