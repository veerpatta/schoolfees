# Components Map

Folder structure: see `docs/maps/folder-map.md`.
Keep this file focused on component folder responsibilities.

- `components/ui`: generic reusable primitives only.
- Module folders: `students`, `imports`, `fees`, `office`, `admin`, `staff`, `branding`.

Planned naming target includes module-aligned folders (`dashboard`, `payments`, `transactions`, `defaulters`, `exports`, `admin-tools`, `receipts`) while preserving existing behavior.
