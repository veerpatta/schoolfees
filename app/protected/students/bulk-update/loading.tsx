import { RouteLoading } from "@/components/admin/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Bulk update"
      description="Loading classes and fields."
      variant="cards"
    />
  );
}
