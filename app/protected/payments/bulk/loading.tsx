import { RouteLoading } from "@/components/admin/route-loading";

export default function Loading() {
  return (
    <RouteLoading
      title="Bulk payment upload"
      description="Preparing the guided upload workflow."
      variant="cards"
    />
  );
}
